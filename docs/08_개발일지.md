# CloneCV 개발 일지

버전: v1.0 | 작성일: 2026-07-06

멀티 PC·멀티 Agent 세션 간 **작업 맥락을 Git으로 공유**하기 위한 로그입니다.  
매 세션 종료 시 Agent에게 "개발일지 갱신"을 요청하세요.

---

## 템플릿 (복사용)

```markdown
### YYYY-MM-DD — [한 줄 요약]

- **목표:**
- **완료:**
  -
- **변경 파일:**
  -
- **지시 요약:** (어떤 프롬프트로 무엇을 시켰는지)
- **AI 사용:**
  - Claude Code:
  - Gemini:
  - 직접:
- **테스트:**
- **남은 TODO:**
  -
- **알려진 이슈:**
  -
- **커밋:** `(hash)` 메시지
```

---

## 2026-07-05 — MVP 초기 구축 (1~7단계)

- **목표:** CloneCV MVP 전 기능 구현
- **완료:**
  - Next.js + Supabase + shadcn/ui 프로젝트 스캐폴딩
  - DB 마이그레이션 5종, 이메일/Google OAuth
  - 슬러그 온보딩, 4단계 이력서 빌더 + 자동 저장
  - 시스템 프롬프트 생성 API, 공개 프로필 + Gemini 챗봇
  - 소유자 대시보드, 랜딩, 소셜 공유, 신고/관리자 모더레이션
- **커밋:** `0be0809` feat: MVP 마무리 및 카카오 공유 imageUrl 수정

---

## 2026-07-05 ~ 2026-07-06 — MVP 이후 기능 확장

- **완료:**
  - README 스크린샷·서비스 소개 추가 (`e987053`)
  - 이력서 데이터 모델 확장: 경력·학력·자격증·자기소개·연락처 (`6419453`)
  - 연속 스크롤 빌더 UI, 섹션 토글 (`a40ae8a`, `20260705210000`)
  - 공개 프로필 경력/학력/연락처 레이아웃 (`a475034`)
  - 챗봇 답변 품질, 동적 추천 질문, birth_year 나이 표시 (`29ff59d`, `ea2b5e4`, `efbc40a`)
  - 브랜드 토큰·다크모드·랜딩 개편 (`754a7f0`)
  - 예상 질문 답변(FAQ), 완성도 카드 (`b2adc54`, `20260706000000`)
  - 전역 네비게이션 UX (`f305040`)
  - PDF 다운로드·업로드 자동 채우기 (`07c8f7b`)
- **커밋:** `e55d206` (최신) settings: devtools-indicator 비활성화

---

## 2026-07-06 — 산출물 최신화 및 멀티 PC 워크플로 정립

- **목표:** 1단계 현황 감사 → 산출물 최신화 → README 기능 정리
- **완료:**
  - `docs/07_현황감사.md` 신규 작성 (코드 vs 명세 감사)
  - `docs/08_개발일지.md` 신규 작성 (본 파일)
  - `docs/02`, `04`, `05`, `06` MVP 이후 내용 반영
  - `README.md` 전면 개편 (프로젝트 설명 + 기능별 스크린샷)
  - `e2e/readme-screenshots.spec.ts` — 공개 페이지 스크린샷 6장 갱신
- **Agent 지시 요약:** "1단계부터 순서대로 작업, README에 구현 기능 스크린샷·설명 정리"
- **테스트:** `npx playwright test e2e/readme-screenshots.spec.ts` 통과
- **남은 TODO:**
  - GitHub Actions CI 구성
  - E2E 통합 시나리오 확장
  - 대시보드·빌더·온보딩 스크린샷 (인증 필요, 수동)
- **커밋:** `a346aee` (산출물 최신화 + CI, 동일 커밋에 포함)

---

## 2026-07-06 — E2E 통합 시나리오 확장

- **목표:** backlog 1순위 — 로그인 → 발행 → 채팅 E2E
- **완료:**
  - `e2e/integration.spec.ts` + helpers (auth, resume-builder, env)
  - Playwright project 분리: smoke / integration / readme-screenshots
  - CI: e2e-smoke job 추가, secrets 있을 때 e2e-integration job
  - `.env.local.example` E2E 변수 문서화
- **테스트:** smoke 5 passed; integration은 `E2E_TEST_*` 없으면 skip
- **남은 TODO:**
  - GitHub Secrets 등록 후 CI integration 검증
  - middleware → proxy 마이그레이션
- **커밋:** `06dbc27` test: E2E 통합 시나리오 및 CI smoke job 추가

---

## 2026-07-08 — 한국 시장 기능·예시 프로필·문서 정리

- **목표:** 한국 시장 맞춤 기능 마무리, 예시 보기 분리, README/docs 최신화
- **완료:**
  - PII 토글, 직접 문의, 모의 면접, FAQ 루프, 섹션 드래그 정렬
  - RSS/GitHub 외부 연동 **제거** → `profile_links`(이름+URL)로 대체
  - 랜딩 예시: `@kimdev` 가상 프로필(김개발) 고정, 실제 사용자 데이터 미노출
  - 채팅 인사말 단순화
  - README·`02_기능명세서`·`07_현황감사` 전면 갱신
- **변경 파일:**
  - `lib/example/demo-profile.ts`, `app/(marketing)/page.tsx`
  - `supabase/migrations/20260707000000_*`, `20260708000000_*`
  - `README.md`, `docs/02_*.md`, `docs/07_*.md`
- **테스트:** `npm run build`, `npm run test` (24 passed)
- **남은 TODO:**
  - `npx playwright install` 후 스크린샷 갱신
  - 빌더·대시보드 캡처 (로그인 필요)
- **알려진 이슈:** 예시 프로필 채팅은 DB에 대화 저장 안 함 (의도된 동작)

---

## 2026-07-06 — middleware→proxy 마이그레이션 및 docs/01 PDF 정합성

- **목표:** backlog 1~2번 (proxy 마이그레이션, 요구사항 PDF Out of Scope 정리)
- **완료:**
  - `middleware.ts` → `proxy.ts`, export `proxy()` (Next.js 16)
  - `docs/01` §5 Out of Scope에서 PDF 제거, §5-1 FR-7.1·7.2 추가
  - README·07 backlog 갱신
- **테스트:** `npm run build`, `npm run test:e2e` (smoke)
- **커밋:** `7c9ed96` refactor: Next.js 16 proxy 마이그레이션 및 docs/01 PDF 정합성

---

## 2026-07-06 — GitHub Actions CI 파이프라인

- **목표:** backlog 1순위 CI 구성
- **완료:**
  - `.github/workflows/ci.yml` 추가 — `push`/`PR` 시 lint · Vitest · build
  - Node 20, npm cache, 빌드용 placeholder env vars
  - README·07 backlog 갱신
- **테스트:** 로컬 `npm run lint`, `npm run test`, `npm run build` 통과
- **남은 TODO:**
  - E2E 통합 시나리오 확장 (backlog 1순위)
  - middleware → proxy 마이그레이션
- **커밋:** `a346aee` ci: GitHub Actions CI 파이프라인 및 프로젝트 문서 최신화

---

## 2026-07-09 — CloneCV A–Z 학습 가이드

- **목표:** TS/Next 초보가 제품→라우팅→연동→채팅→기능 추가까지 따라갈 학습 커리큘럼을 레포에 고정
- **완료:**
  - `docs/09_학습가이드.md` 신규 (Phase 0~8, 스택 선택 이유, 검증·Agent 질문 템플릿)
  - README 문서 표, `AGENTS.md`, `docs/06` §8, `docs/07` 알려진 제한에 링크
- **변경 파일:**
  - `docs/09_학습가이드.md`
  - `README.md`, `AGENTS.md`
  - `docs/07_현황감사.md`, `docs/08_개발일지.md`
- **Agent 지시 요약:** A–Z 학습 계획 구현 (plan 파일은 수정하지 않음)
- **테스트:** 문서 링크·Phase 파일 경로 존재 여부 확인
- **남은 TODO:**
  - 학습자가 Phase 0부터 순서대로 진행하며 Agent에 Phase N 질문
  - (선택) Phase 8 연습: 대시보드 ‘오늘 채팅 횟수’
- **커밋:** (미커밋 — 사용자 요청 시)

---

## 2026-07-20 — 보안 hardening (커밋됨, 일지 누락분 보충)

- **목표:** MVP 보안 강화
- **완료:**
  - RLS insert lockdown, RPC revoke (`20260715100000_mvp_security_hardening.sql`)
  - `lib/auth/safe-redirect.ts` — 오픈 리다이렉트 차단 (+테스트)
  - `lib/public-profile/sanitize-public-profile.ts` — 공개 응답 PII strip (+테스트)
  - `lib/analytics/view-rate-limit.ts`, `lib/inquiries/rate-limit.ts` — API 레이트리밋
  - `DELETE /api/profiles/[profileId]` (UI 미연결)
  - `e2e/multi-profile.spec.ts`
- **커밋:** `fb42407` feat: 보안 hardening과 관측성 기반 추가

---

## 2026-08-21 — 재개용 현황 정리 (코드 변경 없음)

- **목표:** 약 한 달 공백 후 개발 재개 지점 정리
- **완료:**
  - `npm run build` / `npx tsc --noEmit` / `npm run test` 29/29 통과 확인
  - 미커밋 변경 분류: (a) Gemini 모델 선택 env 리팩터, (b) Next 16.3.1 업그레이드
  - `docs/07_현황감사.md` v1.3 — §5 실제 상태, §6 작업 중 항목, §7 backlog 재정렬
- **작업 중 (미커밋):**
  - `lib/chat/constants.ts`, `components/public-profile/chat-panel.tsx`,
    `lib/chat/handle-chat-request.ts`, `next.config.ts`, `.env.local.example`
  - `package.json` / `package-lock.json` (next `16.2.10` → `^16.3.1`)
- **남은 TODO:** `docs/07` §6 미커밋 변경 마무리 → §7 backlog 순서대로
- **커밋:** (미커밋 — docs만 변경)

---

## 2026-09-11 — Cursor → Claude Code 전환

- **목표:** Cursor 전용 설정을 걷어내고 Claude Code 자산으로 통일
- **완료:**
  - 삭제: `.cursor/`, `.cursorrules`, `CURSOR.md`, `docs/06_Cursor_시작프롬프트.md`
    (`.cursorrules`·`karpathy-guidelines.mdc`는 `CLAUDE.md`와 같은 내용의 복사본이었음)
  - Skill 3종을 `.cursor/skills/` → `.claude/skills/` 로 이전 (`/doc-updater`, `/code-quality`, `/pr-helper`)
  - `.cursor/rules/code-quality.mdc` → code-quality 스킬에 흡수,
    `agent-roles.mdc` → `CLAUDE.md` §5
  - `CLAUDE.md` §5 신설 — Next.js 16 주의, 에이전트 역할, 프로젝트 체크, 자주 쓰는 명령
  - `.claude/settings.json` — lint·test·build·typecheck·git 읽기 명령 허용목록
  - `AGENTS.md`, `README.md`, `docs/02·03·05·07·08·09`의 Cursor 참조 정리
- **테스트:** `npm run build` exit 0 · `npm run test` 29 passed
- **남은 TODO:** `docs/07` §6 미커밋 변경 마무리 → §7 backlog
- **커밋:** (미커밋)

---

## 2026-09-18 — 챗봇 추천질문·답변 품질 고도화

- **목표:** 추천 질문을 눌렀는데 클론이 답하지 못하는 문제 제거
- **문제 진단:**
  - `suggested-questions.ts`가 `DEFAULT_SUGGESTED_QUESTIONS`("가장 어려웠던 프로젝트" 등)와
    "강점은?"을 **데이터 유무와 무관하게 무조건** 추천 → 트러블슈팅이 비면 확정 거절
  - `projects[0].title`만 보고 "역할과 성과"를 물어 `role`/`results`가 비어도 추천
  - `topVisitorQuestions`는 방문자가 물었다는 사실만으로 재추천 (실패한 질문 포함)
  - 꼬리질문은 직전 답변만 보고 LLM이 심화 질문을 만들어 구조적으로 이력서 밖으로 이탈.
    "이력서로 답할 수 있어야 한다"는 프롬프트 문구만 있고 **검증 코드 없음**
  - 클라이언트가 `setQuestions(payload.questions)`로 초기 추천을 통째로 덮어써 복구 경로 없음
  - `chat_messages`에 성공 여부가 없어 실패를 집계할 수 없음
- **완료:**
  - `lib/chat/question-coverage.ts` — 결정론적 커버리지 엔진. 근거 필드가 채워진 질문만 생성.
    `buildSystemPrompt`와 같은 `isPromptSectionEnabled` 게이팅을 써서 끈 섹션은 질문이 되지 않음
  - `lib/chat/korean-text.ts` — `match-owner-faq.ts`의 토큰화·의도 그룹을 공유 모듈로 분리
  - `lib/chat/follow-up.ts` — LLM 꼬리질문을 근거로 걸러내고 부족분을 커버리지 질문으로 채움.
    직전 답변이 거절이면 Gemini 2차 호출 자체를 건너뜀
  - `system_prompts.coverage`(jsonb)에 발행 시 저장 → 채팅 경로 추가 DB 조회 0회
  - `chat_messages`에 `answer_status`/`injection_kind`/`model`/`origin` 계측 추가
  - 통계 탭 **답변하지 못한 질문** 카드 + 프로필 탭 **챗봇이 답하기 어려운 영역** 카드
  - `DEFAULT_SUGGESTED_QUESTIONS` 제거, 방문자 재추천은 `answer_status='answered'`만
- **변경 파일:** `lib/chat/{question-coverage,follow-up,korean-text,handle-chat-request,match-owner-faq,sensitive-filter}.ts`,
  `lib/public-profile/{suggested-questions,queries,from-form-values}.ts`, `lib/dashboard/{top-questions,queries,types}.ts`,
  `lib/prompt/generate-system-prompt.ts`, `lib/example/demo-profile.ts`,
  `components/{public-profile/chat-panel,dashboard/stats-tab,dashboard/profile-management-tab,resume-builder/chat-coverage-card}.tsx`,
  `app/(app)/dashboard/page.tsx`, `supabase/migrations/20260918100000_chat_quality_metrics.sql`, `types/database.ts`
- **지시 요약:** "추천질문과 답변 고도화 — 답변 못하는 질문을 추천하고, 꼬리질문이 계속 답이 안 되는 상황"
  → 계획 수립 후 승인받아 구현. 근거 판정은 임베딩 대신 **결정론적 커버리지 규칙**으로 결정(테스트 가능·쿼터 0)
- **테스트:** `npx tsc --noEmit` ✅ · `npm run test` **180 passed / 30 files** (신규 15) · `npm run lint` ✅ · `npm run build` ✅
  - 기존 테스트 2건이 옛 버그(제목만 있는 프로젝트로 질문 생성)를 검증하고 있어 새 계약으로 교체
- **남은 TODO:**
  - 마이그레이션 미적용 — `npm run db:push` 후 `npm run db:types`로 손으로 넣은 컬럼 대조
  - 수동 확인: 트러블슈팅 비운 프로필 재발행 → 해당 추천 질문 사라지는지 / 거절 후 다음 추천이 복구되는지
- **알려진 이슈:** `npm run format:check`는 이전부터 repo 전역 228개 파일 실패 — 이번엔 손댄 파일만 정리
- **커밋:** `349ec84` feat: 챗봇 추천질문 생성 로직 개선

---

## 2026-09-18 — README 최신화 (챗봇 화면 누락 보완)

- **목표:** README 기능 설명에서 챗봇 화면이 아예 보이지 않던 문제 해결 + 현재 구현 상태 반영
- **원인:** 채팅 패널이 플로팅 런처 뒤에 있어 `04-public-profile-desktop.png`(fullPage)에 잡히지 않음.
  스크린샷 스펙에 채팅 캡처 자체가 없었음
- **완료:**
  - `e2e/readme-screenshots.spec.ts` — `13-public-profile-chat.png` 추가.
    런처 열기 → 추천 질문 펼치기 → 뷰포트 캡처 (메시지는 보내지 않음: Gemini 쿼터·비결정성)
  - 통계 캡처가 800px 폴드에서 잘려 **답변하지 못한 질문** 카드가 안 보이던 문제 수정.
    `fullPage: true`는 뷰포트 리사이즈로 Recharts 진입 애니메이션을 재시작시켜 막대가 0으로 찍히므로,
    뷰포트를 1280×1900으로 키우고 막대 높이가 멈출 때까지 폴링한 뒤 캡처
  - README: 채팅 스크린샷 섹션 신설, 추천/꼬리질문 근거 검증 설명, 통계·프로필 탭 신규 카드,
    마이그레이션 23 → 24개, backlog 1건 추가
  - README의 "FAQ 매칭 시 Gemini 호출 없이 즉답" 서술을 실제 구현(프롬프트에 주입 후 재서술)에 맞게 정정
- **변경 파일:** `README.md`, `e2e/readme-screenshots.spec.ts`,
  `docs/screenshots/{11-dashboard-stats,13-public-profile-chat}.png`, `docs/07_현황감사.md`, `docs/08_개발일지.md`
- **지시 요약:** "README 스크린샷 기능설명에 챗봇이 표출 안 됨 — 구현 진행상황에 맞게 최신화"
- **테스트:** `npm run test:e2e:screenshots` ✅ (13개 캡처, 채팅 창·통계 차트 육안 확인)
- **남은 TODO:** `docs/07`은 §3·§5·§6이 2026-08-21 기준으로 남아 있음 — 별도 세션에서 전면 갱신 필요
- **커밋:** (미커밋 — 사용자 요청 시)

---

## 2026-09-18 — 프로필 삭제 UI · AI 사용량 위젯 (단기 backlog 2건)

- **목표:** `docs/07` §7에 남아 있던 "프로필 삭제 UI"와 "Gemini 사용량 모니터링" 처리
- **조사에서 드러난 것:**
  - `DELETE /api/profiles/[profileId]`는 이미 완성(소유권 2중 검증·마지막 프로필 가드)인데 **호출하는 클라이언트 코드가 0건**
  - API가 Storage를 지우지 않음 → `profiles` 행이 사라지면 RLS 술어가 영구 false가 되어 **소유자도 못 지우는데 공개 URL은 계속 열림**
  - **사용량 위젯이 쓸 데이터가 아예 없었음**: `usageMetadata`를 읽는 코드 0건, 레이트리밋은 직접 `INCR` 구현이라 `remaining` 없음, 방문자 키는 IP별이라 프로필 단위 합산 불가
- **완료:**
  - `components/dashboard/delete-profile-dialog.tsx` — 슬러그 재입력 확인. 마지막 프로필은 API 400 + UI `disabled` 이중 차단, demoMode 미노출
  - `lib/profile/delete-storage.ts` — **행 삭제 전에** 두 버킷 정리. 실패해도 삭제는 막지 않고 `storageCleanupFailed`로 경고
  - `lib/chat/usage-metadata.ts` — 스트림 마지막 청크의 `usageMetadata`를 `unknown`에서 좁혀 파싱(`any` 0건). 꼬리질문 호출분까지 합산해 저장
  - `lib/chat/usage-counter.ts` + `rateLimitKey`/`rateLimitBucket` 추출 — 쓰기와 읽기가 **같은 키 생성 함수**를 쓰도록 고정
  - `lib/dashboard/ai-usage.ts` + `components/dashboard/ai-usage-card.tsx` — 실측/추정 분리 표기, Redis 미설정 시 fail-open("측정 불가")
  - `system_prompts.token_estimate`의 첫 소비처가 생김 (README backlog 1건 해소)
- **변경 파일:** `supabase/migrations/20260919100000_chat_token_usage.sql`, `lib/chat/{usage-metadata,usage-counter,gemini-stream,handle-chat-request}.ts`,
  `lib/{rate-limit/redis,dashboard/ai-usage,profile/delete-storage}.ts`, `app/api/profiles/[profileId]/route.ts`,
  `components/dashboard/{delete-profile-dialog,ai-usage-card,stats-tab,profile-management-tab,dashboard-tabs}.tsx`, `types/database.ts`
- **지시 요약:** "단기 백로그 2건 구현 — 프로필 삭제 UI 연결, AI 사용량/쿼터 위젯" → 계획 승인 후 구현.
  토큰은 **실측 캡처 + 추정치 병행**, 레이트리밋은 **프로필 단위 카운터 신설**, Storage 정리 **포함**으로 결정
- **테스트:** `npx tsc --noEmit` ✅ · `npm run test` **196 passed / 33 files**(신규 16) · `lint`/`build` ✅
- **남은 TODO:** 마이그레이션 적용, 실제 대시보드에서 삭제·토큰 적재 수동 확인
- **커밋:** `40894de`

---

## 2026-09-18 — 메인페이지 · 대시보드 UI/UX 개편

- **목표:** 서비스 첫인상과 대시보드 사용성 개선 (뷰 레이어 전용, DB·API·RLS 무변경)
- **전제 정정:** `components/landing/`은 존재하지 않고 랜딩 전체가 `app/(marketing)/page.tsx` 한 파일(227줄)에 인라인.
  `components/ui/`에 shadcn 컴포넌트가 7개뿐이라 Drawer·Badge·Progress 없음(@base-ui 기반)
- **완료:**
  - `components/marketing/hero-chat-demo.tsx` — **대본 재생** 데모(`/api/chat` 호출 0건). 랜딩은 최다 트래픽 페이지라 방문자당 5회/분 제한과 무료 쿼터를 쓸 수 없음. reduced-motion이면 즉시 전체 표시
  - `components/marketing/reveal.tsx` — IntersectionObserver 스크롤 등장. React state 대신 DOM 속성 토글
  - 통계 카드는 **제품 사실 기반**(빌더 10섹션·예상 질문 20개·3분·무료) — 없는 사용자 수·후기를 지어내지 않음
  - `components/dashboard/profile-publish-bar.tsx` — 공개 주소·복사·공개 토글을 프로필 탭에서 꺼내 **상시 고정**(모든 탭에서 보임)
  - `components/dashboard/dashboard-overview.tsx` + `lib/dashboard/readiness.ts` — 추가 쿼리 없이 4개 타일, 타일에서 해당 탭으로 이동
  - 탭을 제어형으로 전환, 모바일은 기존 `Select` 재사용(신규 UI 프리미티브 0개)
- **변경 파일:** `app/(marketing)/page.tsx`, `components/marketing/{hero-chat-demo,reveal}.tsx`,
  `components/dashboard/{profile-publish-bar,dashboard-overview,dashboard-tabs,profile-management-tab}.tsx`,
  `lib/dashboard/readiness.ts`, `e2e/smoke.spec.ts`
- **지시 요약:** "메인페이지 & 대시보드 UI/UX 전면 개편" → Hero 데모는 **대본만**, Overview는 **탭 위 상시 섹션**,
  모바일 탭은 **Select 재사용**, 통계는 **제품 사실 기반**으로 결정
- **테스트:** `tsc`/`lint`/`build` ✅ · `npm run test` **201 passed / 34 files**(신규 5)
  - 브라우저 확인: 랜딩 `/api/chat` 요청 0건 단언, 타일→탭 전환, 375px 반응형
- **주의:** CTA 라벨 변경으로 `e2e/smoke.spec.ts` 셀렉터 1줄 수정. h1은 정규식 계약(`대화하는 AI 이력서`) 유지
- **커밋:** `40894de` (위 항목과 같은 커밋)

---

## 2026-09-18 — 공개 프로필 디자인 커스텀 시스템 (2단계 중 Step 1)

- **목표:** 소유자가 공개 프로필의 색·글꼴·카드·아바타 모양을 고르고 실시간으로 확인
- **전제 정정:** 공개 프로필은 `app/(public)/[slug]`가 아니라 **`app/[id]/page.tsx`**(`/@slug`는 `proxy.ts` rewrite).
  `lib/types/profile.ts` 없음. **Pretendard 미설치**
- **완료:**
  - `profiles.theme_config`(jsonb, nullable) + `lib/types/profile.ts` — 기본값은 DB가 아니라 코드(`DEFAULT_THEME_CONFIG`). `parseThemeConfig`는 null·부분 객체·쓰레기 값을 전부 흡수
  - `lib/theme/css-vars.ts` — `globals.css` `:root`와 **같은 토큰명**을 덮어써서 하위 컴포넌트를 한 줄도 고치지 않고 적용. accent 대비색은 WCAG 상대휘도로 계산
  - 주입 지점은 `public-profile-body.tsx` **단 한 곳** → 공개 페이지·빌더 미리보기·커스터마이저가 동시에 커버됨
  - 디자인 탭(40:60) + **전체 화면 편집 모드**. 미리보기는 목업이 아니라 실제 `PublicProfileBody`
  - 아바타 원형은 **상단 기준 정사각 크롭** — `AVATAR_ASPECT`·크롭 다이얼로그·저장 픽셀을 건드리지 않아 기존 사진이 재크롭되지 않음
- **고친 기존 버그 2건:**
  - `globals.css`의 `--font-sans: var(--font-sans)` 자기 참조 → **Geist가 한 번도 적용된 적 없었음**. Pretendard를 앱 기본 글꼴로 연결(Geist는 `subsets: ["latin"]`이라 한글 글리프 없음)
  - 모바일 미리보기가 컨테이너만 390px로 줄여 **Tailwind `lg:`가 실제 뷰포트 기준으로 걸림** → 목차 레일이 뜨고 본문이 한 글자씩 접힘. `PublicProfileBody`에 `narrow` prop 추가(기본 false라 실제 페이지 무영향)
- **변경 파일:** `supabase/migrations/20260920100000_profile_theme_config.sql`, `lib/types/profile.ts`, `lib/theme/css-vars.ts`,
  `components/dashboard/design-customizer/*`(4), `components/public-profile/{public-profile-body,profile-hero,resume-panel}.tsx`,
  `app/api/profile/theme/route.ts`, `app/layout.tsx`, `app/globals.css`, `lib/public-profile/{types,queries,from-form-values}.ts`
- **지시 요약:** "2단계 연속 고도화 — Step 1 디자인 커스텀, Step 2 JD 갭 분석" → 계획 승인 후 **Step 1만** 구현.
  `sectionOrder`는 theme_config에서 제외(기존 `profiles.section_order`가 소유), 폰트는 next/font 번들, 원형은 상단 크롭으로 결정
- **신규 의존성:** `pretendard` 1개 (`next/font/local`이 node_modules의 woff2 참조 — 저장소에 바이너리 없음)
- **테스트:** `tsc`/`lint`/`build` ✅ · `npm run test` **212 passed / 35 files**(신규 11)
  - 브라우저 확인: 프리셋 5종 전환, 전체 화면 편집, 모바일 토글, 컨트롤 변경 시 저장 요청 0건 단언
- **남은 TODO:** 마이그레이션 적용 후 실제 저장 확인 · **Step 2(JD 갭 분석) 승인 대기**
- **커밋:** `a9a5362`

---

## 다음 세션 시작 프롬프트

```
docs/07_현황감사.md, docs/08_개발일지.md, README.md, docs/09_학습가이드.md 를 읽고
개발일지 마지막 항목 이후로 이어서 작업할 거야.
먼저 npm run db:push 로 미적용 마이그레이션 3건 적용 + npm run db:types
(손으로 넣은 컬럼과 diff가 없어야 정상 — docs/07 §6 참고).
그다음 Step 2(JD 갭 분석·맞춤 이력서 제안) 또는 docs/07 §7 backlog.
학습은 Phase N부터.
```
